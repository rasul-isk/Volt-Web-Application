defmodule Volt.MenusFixtures do
  @moduledoc """
  This module defines test helpers for creating
  entities via the `Volt.Menus` context.
  """

  # @doc """
  # Generate a menu.
  # """
  # def menu_fixture(attrs \\ %{}) do
  #   {:ok, menu} =
  #     attrs
  #     |> Enum.into(%{
  #       numberOfItems: 42
  #     })
  #     |> Volt.Menus.create_menu()

  #   menu
  # end
end
